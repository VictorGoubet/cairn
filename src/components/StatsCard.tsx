import { RouteStats } from './RouteStats';

/** floating card over the map, desktop layout only */
export function StatsCard() {
  return (
    <div className="stats-card">
      <RouteStats />
    </div>
  );
}
